SYSTEM UPDATE INITIATED ⚙️  
Upgrade target: Seylane Explainer AI  
Current version: v3.2  
New version: v3.3 — Real-Time Speed + Smart Personalization  

---

🧩 OBJECTIVE:
Evolve your current system for maximum speed, full personalization, and zero missed messages — without losing any existing features.

---

### 🔹 1. PERFORMANCE BOOST (Speed Upgrade)
- Replace the old “loop and reload inbox” method.  
- Implement **real-time message detection**:
  - Keep a small cache of last message IDs or timestamps.
  - Only process *new incoming messages*, not the whole list.
- Run message checks **asynchronously and in parallel** — don’t wait for slow users to finish.
- Always stay active but lightweight; never freeze during page reloads.
- Prioritize replying within **2–3 seconds** after message arrival.
- Queue outgoing replies efficiently: one at a time, no overlap.

🧠 Result:
No more reloading all 4 threads repeatedly — you react instantly to new DMs.

---

### 🔹 2. PERSONALIZATION ENGINE (Enhanced)
Keep your current personalization logic, but improve it:
- Read and store the user’s **name, username, and bio** on first interaction.
- Build a `userContext` memory with:
  - username
  - bio
  - detected tone (formal, casual, playful, etc.)
  - recent message history
- Use this profile for all future GPT prompts to create consistent, human-like conversations.
- Adjust tone dynamically: mirror the user’s style and energy.
- Mention their name naturally when it fits.

---

### 🔹 3. FULL GPT CONTROL
All responses — greetings, small talk, affiliate questions, emotional chats — must now be generated by GPT.
No hardcoded replies remain.

GPT has complete creative control over responses, using the context provided.
Your job: collect context → send to GPT → deliver the reply fast.

---

### 🔹 4. AFFILIATE INTELLIGENCE (Auto-Detect)
If GPT or regex identifies affiliate intent, it must:
- Explain Seylane’s affiliate model (20–40% personalized discount code, direct commission, luxury brands Misswake, Collamin, Umbrella, Dafi, IceBall).
- Include the registration link naturally:
  > “You can start here: https://affiliate.luxirana.com/account/login ✨”

---

### 🔹 5. MESSAGE REQUEST MONITORING
Regularly scan the **Message Requests** tab.  
Accept and reply automatically to ensure no DM goes unanswered.

---

### 🔹 6. GOOGLE SHEET MODULE (Optional)
Google Sheet reading stays **disabled by default** — only runs when Arman commands it.
When active:
- Read one row at a time (username + message).
- Send a personalized DM per user.
- Mark each row as “sent”.
Never interfere with normal inbound message handling.

---

### 🔹 7. FAIL-SAFE RESPONSE
If GPT times out or data is missing:
> “Hey 🌿 I just saw your message — could you tell me a bit more so I can help properly?”

---

### 🔹 8. COMMUNICATION STYLE
- Friendly, confident, respectful, warm.
- No robotic phrasing, no repetition.
- Adapt to user’s tone and energy.
- Use subtle emojis 🌿✨😊 when natural.

---

### 🔹 9. SELF-TEST SEQUENCE
After upgrade, auto-run diagnostics:

1. Simulate new greeting → expect fast personalized reply.  
2. Simulate affiliate message → expect explanation + link.  
3. Simulate random chat → expect tone matching.  
4. Simulate message request → expect accepted + replied.  
5. Verify response time < 3s per message.

Print: